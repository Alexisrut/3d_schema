# Что НЕ должно попасть в сборку фронтенда.
#
# node_modules обязательно исключить: их ставит `npm ci` внутри образа под
# нужную платформу. Скопированные с macOS модули с бинарными зависимостями
# в linux-контейнере просто не запустятся.

node_modules/
dist/
.vite/

# Результаты локальных прогонов.
*.local
.DS_Store
